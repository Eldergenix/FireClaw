# bridge/ — Python interop layer
#
# All Python library dependencies are isolated here.
# Other packages import bridge modules for networking, OAuth, regex, etc.
# Each bridge module provides a pure-Mojo interface wrapping Python libs.
